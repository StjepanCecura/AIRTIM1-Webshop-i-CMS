import React from "react"
import { ICarousel } from "../types/carousel.type"
import Spinner from "../components/Spinner"

const Carousel = ({ carouselData }: { carouselData: ICarousel }) => {
  return <div>Carousel</div>
}

export default Carousel
